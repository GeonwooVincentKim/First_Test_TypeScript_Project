export const arrow1 = (a: number, b: number): number => {return a + b}
export const arrow2 = (a: number, b: number): number => a + b

export function f(){
    let x = 1, y = 2
    let result = x + y + 10
}

export function isGreater(a: number, b: number) : boolean { 
    return a > b
}
